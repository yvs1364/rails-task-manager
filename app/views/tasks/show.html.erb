<h1><%= @task.title %></h1>
<p><%= @task.details %></p>

<%= link_to "🔙", tasks_path %>
<%= link_to "✏️", edit_path(@task) %></li>

<%= link_to "🚮", task_path(@task),
      method: :delete,
      data: { confirm: "Are you sure?" } %>

